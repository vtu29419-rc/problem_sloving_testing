class Solution:
    def removeElement(self, nums, val):
        k = 0
        n = len(nums)

        while k < n:
            if nums[k] == val:
                nums[k] = nums[n - 1]
                n -= 1
            else:
                k += 1

        return n
